// Author: Artem Sapegin, http://sapegin.me, 2013

body {
	background:bg-color;
	color:base-color;
	line-height:1.5;
	padding:spacer 2*spacer;
	font-size:normal-font;
	if (max-width) {
		max-width:max-width;
		margin-left:auto;
		margin-right:auto;
	}
}


// Fonts

body { font-family:"Helvetica Neue", Arial, sans-serif; }


// Headers: alpha, beta, gamma, delta, epsilon, epsilon

.alpha {
}


// Text

.text {
	a {
		@extend .link;
	}

	p,
	ul,
	ol {
		space(2);
	}
}


.header {
	clearfix();
	space(2);
}

.logo {
	float:left;
	// margin-right:2em;

	&,
	&__link,
	&__link:link,
	&__link:visited,
	&__link:hover,
	&__link:active,
	&__link:focus {
		font-size:normal-font;
		color:base-color;
		.page_content & {
			color:light-color;
		}
		.page_content &:hover,
		.page_content &:active,
		.page_content &:focus {
			color:#fff;
		}
	}
}

.nav {
	float:right;
	// float:left;

	li {
		display:inline-block;
		margin-left:.6em;
		&:first-child {
			margin-left:0;
		}
	}

	a,
	a:link,
	a:visited,
	a:hover,
	a:active,
	a:focus {
		font-size:normal-font;
		color:base-color;
		.page_content & {
			color:light-color;
		}
		.page_content &:hover,
		.page_content &:active,
		.page_content &:focus {
			color:#fff;
		}
	}
}

.footer {
	.page_content & {
		display:none;
	}
}

.page {
	&_content {
		background:#1a1a1a;

		.content {
			max-width:100%;
		}
	}
}

.lightbox {
	position:relative;

	&__gallery {
		position:absolute;
		top:40px;
		bottom:spacer;
		left:spacer;
		right:spacer;
		background-repeat:no-repeat;
		background-position:center center;
		background-size:contain;
		&.is-activated {
			background:none !important;
		}
	}

	&__controls {
		absolute:left 0 bottom 0;
		z-index:9999;
		opacity:0;
		transition:opacity 333ms cubic-bezier(0.1, 0.25, 0.25, 1);
	}

	&:hover &__controls {
		opacity:1;
	}

	.js &__meta {
		display:none;
	}
}

.lightbox-pane {
	&__button {
		width:50px;
		height:50px;
		line-height:50px;
		color:light-color;
		text-align:center;
		cursor:default;
		text-shadow:2px 2px 4px black(.5);
		.icon {
			font-size:20px;
		}
	}

	&__popup {
		display:none;
		absolute:left 50px bottom 0;
		padding:spacer;
		background:#fff;
		// box-shadow:inset 0 0 10px 2px black(.75);
	}

	&:hover &__button {
		// color:#000;
		color:#fff;
		// background:#fff;
		// text-shadow:none;
		text-shadow:0 0 10px hsla(209, 75%, 65%, 1);
		// text-shadow:0 0 10px hsla(0, 100%, 100%, 1)//, 0 0 10px hsla(0, 100%, 100%, 1);
	}
	&:hover &__popup {
		display:block;
	}
}

.photo-info {
	width:800px;

	&__title {
		// space();
		font-size:32px;
		font-weight:normal;
	}

	&__album {
		space(2);
		font-size:small-font;
	}

	&__album-link:link,
	&__album-link:visited,
	&__album-link:hover,
	&__album-link:active,
	&__album-link:focus {
		color:base-color;
		text-decoration:none;

		u {
			text-decoration:underline;
		}
	}

	&__caption{
		space(2);
		font-size:normal-font;
	}

	&__meta {
		font-size:small-font;
		color:#999;
	}
}

.thumbnails {
	margin:0 (-(spacer / 2)) spacer*2;

	&__item {
		display:inline-block;
		margin:spacer (spacer / 2);

		img {
			height:200px;
			width:auto;
		}
	}
}

.thepage {
	max-width:1024px;

	p {
		font-size:normal-font;
		space(2);
	}

	div + p {
		margin-top:4*spacer;
	}
}

.entry-header {
	space(4);
}

.entry-title {
	font-size:48px;
	font-weight:normal;

	&__link {
		&,
		&:link,
		&:visited,
		&:hover,
		&:active,
		&:focus {
			color:base-color;
			text-decoration:none;
		}
		&:hover,
		&:active,
		&:focus {
			border-bottom:2px solid;
		}
	}

	.essay_photo & {
		space(2);
		font-size:22px;

		&__link {
			&,
			&:link,
			&:visited,
			&:hover,
			&:active,
			&:focus {
				color:bg-color;
			}
		}
	}
}

.entry-excerpt {
	font-size:small-font;
}

.entry-pubdate {
	font-size:small-font;
}

.entry-tags {
	font-size:small-font;
}

.entry-instagrams {
	layout-row();
	margin-top:(-(spacer));
	margin-bottom:(-(spacer));

	&__item {
		layout-nth(3);
		margin-top:spacer;
		margin-bottom:spacer;
	}

	&__photo {
		max-width:100%;
	}
}

.entry-photo {
	space(2);

	&__photo {
		max-width:100%;
	}
}


.essay {
	space(4);

	&_photo {
		margin-left:(-(2*spacer));
		margin-right:(-(2*spacer));
		padding:4*spacer 0;
		background:base-color;
		color:bg-color;
	}

	&__content {
		space(2);

		p,
		ul,
		ol {
			max-width:70em;
			line-height:1.7;
		}
	}

	&__pubdate {
		display:inline-block;
		margin-right:1em;
	}
}

.blog-tags {
	space(4);

	&__item {
		display:inline-block;
		margin-right:1em;
		font-size:normal-font;
	}

	&__counter {
		margin-left:.25em;
		font-size:small-font;
	}
}
