.albums-link {
	display: block;
	font-weight: 300;
	+link-all-states() {
		text-decoration: none;
		outline: 0;
	}

	&__albums {
		layout-row();
	}

	&__link {
		color: base_color;
	}
	&:hover &__link u {
		color: dark_hover_color;
	}
}
